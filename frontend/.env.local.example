# Copy this file to .env.local and fill in the values.
# .env.local is gitignored — never commit it.

# Dev API Gateway URL — local frontend (npm run dev) proxies /api/* here.
# This points to the dev environment, which is also where PRs deploy to.
NEXT_PUBLIC_API_URL=https://xfsvqay2a7.execute-api.us-east-1.amazonaws.com
