# Copy this file to .env.local and fill in the values.
# .env.local is gitignored — never commit it.

# Staging API Gateway URL — get this from the ConnectStagingBackendStack outputs.
# Local frontend (npm run dev) proxies /api/* to this URL.
NEXT_PUBLIC_API_URL=https://io4y5nj3dg.execute-api.us-east-1.amazonaws.com
