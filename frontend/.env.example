# Copy this file to .env.local and fill in the values.
# .env.local is gitignored — never commit it.

# Dev API Gateway URL — used by next.config.js to proxy /api/* in dev.
# Get this from the ConnectDevBackendStack outputs.
NEXT_PUBLIC_API_URL=https://xfsvqay2a7.execute-api.us-east-1.amazonaws.com
