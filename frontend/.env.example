# Copy this file to .env.local and fill in the values.
# .env.local is gitignored — never commit it.

# Staging API Gateway URL — used by next.config.js to proxy /api/* in dev.
# Get this from the ConnectStagingBackendStack outputs.
NEXT_PUBLIC_API_URL=https://io4y5nj3dg.execute-api.us-east-1.amazonaws.com
